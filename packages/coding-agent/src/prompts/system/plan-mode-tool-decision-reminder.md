<system-reminder>
Plan mode turn ended without a required tool call.

You MUST choose exactly one next action now:
1. Call `{{askToolName}}` to gather required clarification, OR
2. Write the plan slug/title (`<slug>`, matching `.omp/plans/YYYY-MM-DD-<slug>.md`) as plain text to `xd://propose` with `{{writeToolName}}` to finish planning and request approval

You NEVER output plain text in this turn.
</system-reminder>
